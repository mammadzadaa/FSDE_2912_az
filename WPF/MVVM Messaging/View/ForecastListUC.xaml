<UserControl x:Class="MVVM_Messaging.View.ForecastListUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM_Messaging.View"
             mc:Ignorable="d" 
             d:Background="White"
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
            <Button Command="{Binding AddPage}" Width="250">Add</Button>
            <Button Width="150">Cancel</Button>
        </StackPanel>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"> Main Page </TextBlock>
    </Grid>
</UserControl>
